@model InvoicesViewModel
@{
    ViewBag.Title = "All Invoices";
}

<form method="get">
    <div class="row">
        <div class="form-group col-md-4">
            <label asp-for="MyCompanyName"></label>
            <select asp-for="MyCompanyName" class="form-control">
            <option value="">All</option>
                @foreach (var name in Model.MyCompanyNames)
                {
                    <option value="@name">@name</option>
                }
            </select>
        </div>
    </div>
    <div class="row">
<input type="submit" class="btn btn-info" value="Search"/></div>
<br />
</form>

@{
    var previouspage = Model.CurrentPage - 1;
    if(previouspage < 1)
    {
        previouspage = 1;
    }

    var maxpage = Math.Ceiling((double)Model.TotalInvoices/InvoicesViewModel.InvoicesPerPage);
}

<div class="row mb-5">
    <div class="col-md-6">
        <a class="btn btn-primary @(Model.CurrentPage == 1 ? "disabled" : string.Empty)"
           asp-controller="Reports"
           asp-action="AllInvoices"
           asp-route-currentPage="@previouspage"
           asp-route-customerName="@Model.MyCompanyName"
          ><<</a>
    </div>
    
    @{
        var shouldButtonBeDisabled = Model.CurrentPage == maxpage ||
                                     !Model.InvoiceCollection.Any();
    }

    <div class="col-md-6">
        <a class="btn btn-primary float-sm-right @(shouldButtonBeDisabled ? "disabled" : string.Empty)" style="margin-top: 10px"
           asp-controller="Reports"
           asp-action="AllInvoices"
           asp-route-currentPage="@(Model.CurrentPage + 1)"
           asp-route-customerName="@Model.MyCompanyName"
           >>></a>
    </div>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                Invoice Number
            </th>
            <th>
                Date
            </th>
            <th>
                Customer
            </th>
            <th>
                Customer OC No
            </th>
            <th>
                Delivery Terms
            </th>
            <th>
                Invoice Amount
            </th>
            <th>
                Supplier
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var order in Model.InvoiceCollection)
        {
            <tr>
                <th>
                    @order.DocumentNumber
                </th>
                <th>
                    @order.Date
                </th>
                <th>
                    @order.CustomerName
                </th>
                <th>
                    @order.OrderConfirmationNumber
                </th>
                <th>
                    @order.Incoterms
                </th>
                <th>
                    @order.TotalAmount
                </th>
                <th>
                    @order.SupplierName
                </th>
                <th><a asp-controller="Reports" asp-action="InvoiceDetails" asp-route-id="@order.Id" class="btn btn-info mr-1">Details</a></th>
                <th> <a asp-controller="Reports" asp-action="EditInvoice" asp-route-id="@order.Id" class="btn btn-info mr-1">Edit</a> </th>
            </tr>
        }
    </tbody>
</table>
